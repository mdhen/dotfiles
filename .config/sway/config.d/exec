# Start a dbus session
exec $dbus
# Execute swayidle
exec $idle
# Restore default brightness
exec light -S $brightness
# Start a notification daemon
exec mako
# Start nextcloud
exec nextcloud
# Start gammastep
exec gammastep
# Start kanshi
exec kanshi

